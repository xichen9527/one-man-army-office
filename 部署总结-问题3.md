# 问题3修复：获取会议 Token 失败（Edge Function 返回 non-2xx）

## ✅ 修复状态：已完成

### 修复内容
Edge Function `livekit-token` 已按照修复方案完成修改：

**核心改动：**
1. **使用两个独立的 Supabase client：**
   - `authClient`: 用于验证用户身份（使用 `SUPABASE_ANON_KEY` + Authorization header）
   - `serviceClient`: 用于读取配置（使用 `SUPABASE_SERVICE_ROLE_KEY`，绕过 RLS）

2. **避免 Authorization header 与 service_role 冲突**
   - 之前的问题：同时使用 service_role key 又在 headers 中传 Authorization，导致权限冲突
   - 修复后：验证身份和读取配置使用不同的 client，互不干扰

### 代码位置
`D:\代码\one-man-army-office\supabase\functions\livekit-token\index.ts`

### 部署状态
✅ **已成功部署**
- 部署时间：2026-06-24
- 项目 ID：jikjcdrrcywnwmtaabzh
- 函数名称：livekit-token
- 访问 URL：https://jikjcdrrcywnwmtaabzh.supabase.co/functions/v1/livekit-token

### 测试方法

#### 方法1：通过前端界面测试（推荐）
1. 打开一人成军办公平台
2. 进入"视频会议"功能
3. 创建或加入一个会议
4. 打开浏览器开发者工具（F12）的 Network 标签
5. 查看 `livekit-token` 请求
   - 应返回状态码 **200**
   - 响应内容应包含：`token`, `url`, `roomName`

#### 方法2：通过浏览器 Console 测试
1. 打开浏览器开发者工具（F12）
2. 切换到 Console 标签
3. 运行以下代码获取 JWT token：
```javascript
// 获取当前用户的 JWT token
const token = JSON.parse(localStorage.getItem('sb-jikjcdrrcywnwmtaabzh-auth-token'))?.access_token;
console.log('JWT Token:', token);
```

4. 使用获取的 token 测试 Edge Function：
```javascript
// 测试创建房间
const token = JSON.parse(localStorage.getItem('sb-jikjcdrrcywnwmtaabzh-auth-token'))?.access_token;

fetch('https://jikjcdrrcywnwmtaabzh.supabase.co/functions/v1/livekit-token', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    roomName: 'test-room-' + Date.now(),
    action: 'create',
    participantName: '测试用户'
  })
})
.then(res => res.json())
.then(data => {
  console.log('状态码:', res.status);
  console.log('响应数据:', data);
})
.catch(err => console.error('错误:', err));
```

#### 方法3：使用 Node.js 测试脚本
已创建测试脚本：`D:\代码\one-man-army-office\test-livekit-token.js`

使用方法：
```bash
# 1. 获取 JWT token（从浏览器 localStorage）
# 2. 运行测试脚本
node test-livekit-token.js <your_jwt_token>
```

### 预期结果

#### ✅ 成功情况（状态码 200）
```json
{
  "token": "eyJhbGciOiJSUzI1NiIs...",
  "url": "wss://your-livekit-server.com",
  "roomName": "test-room-123",
  "participantIdentity": "user-uuid",
  "displayName": "用户名"
}
```

#### ❌ 失败情况

**401 Unauthorized - 未认证**
```json
{
  "error": "Unauthorized",
  "message": "用户认证失败，请重新登录"
}
```
原因：JWT token 无效或已过期

**404 CONFIG_NOT_FOUND - 未配置 LiveKit**
```json
{
  "error": "LiveKit not configured. Please configure it in Settings -> Video Conference.",
  "code": "CONFIG_NOT_FOUND"
}
```
原因：用户未配置 LiveKit 服务器信息
解决：在设置页面配置 LiveKit

**500 CONFIG_ERROR - 配置读取失败**
```json
{
  "error": "Failed to read config",
  "details": "...",
  "code": "CONFIG_ERROR"
}
```
原因：数据库查询失败
解决：检查 `video_conference_configs` 表结构和权限

### 验证清单
- [x] Edge Function 代码已修复（使用双 client 方案）
- [x] Edge Function 已成功部署到 Supabase
- [ ] 前端可以成功获取 Token（需要实际测试）
- [ ] 视频会议可以正常创建和加入（需要实际测试）

### 下一步
1. 在前端界面测试视频会议功能
2. 确认能够成功创建和加入会议
3. 如果仍有问题，检查浏览器 Console 的错误信息

### 相关文件
- 源代码：`supabase/functions/livekit-token/index.ts`
- 测试脚本：`test-livekit-token.js`
- 前端调用：`src/features/videoConference/hooks/useVideoConference.ts`
